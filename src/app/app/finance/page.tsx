"use client";
import { useState } from "react";import { useApi,useApiMutation } from "@/hooks/useApi";import { formatCurrency,formatDate,PAYMENT_STATUS } from "@/lib/constants";import { Button } from "@/components/ui/Button";import { Input } from "@/components/ui/Input";import { Select } from "@/components/ui/Select";import { Modal } from "@/components/ui/Modal";import { TableSkeleton } from "@/components/ui/Skeleton";import { EmptyState } from "@/components/ui/EmptyState";import { useToast } from "@/components/ui/Toast";
type Invoice={id:string;clientId:string;clientName:string;description:string;total:number;status:string;dueDate:string};type Client={id:string;name:string};type Analytics={revenue:{paid:number;pending:number;overdue:number;byMonth:{month:string;paid:number}[]}};const blank={clientId:"",description:"",value:"",dueDate:""};
export default function FinancePage(){const [open,setOpen]=useState(false);const [form,setForm]=useState(blank);const {showToast}=useToast();const inv=useApi<Invoice[]>("/api/invoices?pageSize=100");const analytics=useApi<Analytics>("/api/analytics");const clients=useApi<Client[]>("/api/clients?pageSize=100");const create=useApiMutation<Record<string,unknown>>("/api/invoices","POST");async function save(){const c=clients.data?.find(x=>x.id===form.clientId);if(!c||!form.description||!form.value||!form.dueDate)return showToast("Preencha os campos obrigatórios","error");const total=Math.round(Number(form.value)*100);const r=await create.mutate({...form,clientName:c.name,subtotal:total,total,status:"pending"});if(r.success){showToast("Cobrança criada!","success");setOpen(false);setForm(blank);inv.refetch();analytics.refetch()}else showToast(r.error||"Erro ao criar","error")}async function paid(x:Invoice){const r=await fetch(`/api/invoices/${x.id}`,{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify({status:"paid",paidAt:new Date().toISOString()})}).then(x=>x.json());showToast(r.success?"Pagamento registrado!":r.error||"Erro",r.success?"success":"error");if(r.success){inv.refetch();analytics.refetch()}}const a=analytics.data?.revenue;return <div><header className="flex justify-between items-end mb-8"><div><p className="text-xs uppercase tracking-[.25em] text-gold">Fluxo financeiro</p><h1 className="text-3xl text-white font-semibold mt-2">Financeiro</h1></div><Button onClick={()=>setOpen(true)}>Nova cobrança</Button></header><div className="grid grid-cols-3 gap-4 mb-8">{[["Recebido",a?.paid],["Pendente",a?.pending],["Atrasado",a?.overdue]].map(([l,v])=><article key={l} className="p-5 bg-white/[.02] border border-white/5 rounded-2xl"><p className="text-xs text-noir-500">{l}</p><p className="text-xl text-white mt-2">{formatCurrency(Number(v||0))}</p></article>)}</div>{inv.loading?<TableSkeleton/>:inv.error?<Retry error={inv.error} retry={inv.refetch}/>:inv.data?.length?<div className="space-y-3">{inv.data.map(x=>{const info=PAYMENT_STATUS[x.status as keyof typeof PAYMENT_STATUS];return <article key={x.id} className="p-4 bg-white/[.02] border border-white/5 rounded-xl flex flex-col md:flex-row md:items-center gap-4"><div className="flex-1"><h2 className="text-white text-sm">{x.description}</h2><p className="text-xs text-noir-500">{x.clientName} · vence {formatDate(x.dueDate)}</p></div><strong className="text-white">{formatCurrency(x.total)}</strong><span className={`text-xs ${info?.textColor}`}>{info?.label||x.status}</span>{x.status!=="paid"&&<Button size="sm" onClick={()=>paid(x)}>Marcar pago</Button>}</article>})}</div>:<EmptyState title="Nenhuma cobrança" description="Registre sua primeira cobrança." action={<Button onClick={()=>setOpen(true)}>Nova cobrança</Button>}/>}<Modal isOpen={open} onClose={()=>setOpen(false)} title="Nova cobrança"><div className="space-y-4"><Select label="Cliente *" value={form.clientId} onChange={e=>setForm({...form,clientId:e.target.value})} placeholder="Selecione" options={(clients.data||[]).map(x=>({value:x.id,label:x.name}))}/><Input label="Descrição *" value={form.description} onChange={e=>setForm({...form,description:e.target.value})}/><Input label="Valor (R$) *" type="number" value={form.value} onChange={e=>setForm({...form,value:e.target.value})}/><Input label="Vencimento *" type="date" value={form.dueDate} onChange={e=>setForm({...form,dueDate:e.target.value})}/></div><div className="flex justify-end gap-2 mt-6"><Button variant="secondary" onClick={()=>setOpen(false)}>Cancelar</Button><Button onClick={save} isLoading={create.loading}>Criar cobrança</Button></div></Modal></div>}
function Retry({error,retry}:{error:string;retry:()=>void}){return <div className="text-center py-16"><p className="text-red-400">{error}</p><Button className="mt-4" onClick={retry}>Tentar novamente</Button></div>}
