"use client";import { PLANS,formatCurrency } from "@/lib/constants";export default function PlansPage(){return <div><header className="mb-8"><p className="text-xs uppercase tracking-[.25em] text-gold">Catálogo</p><h1 className="text-3xl text-white font-semibold mt-2">Planos</h1><p className="text-sm text-noir-500 mt-2">Configuração oficial dos planos NoirFrame.</p></header><div className="grid md:grid-cols-2 xl:grid-cols-4 gap-4">{Object.values(PLANS).map(p=><article key={p.id} className="p-6 bg-white/[.02] border border-white/5 rounded-2xl"><p className="text-xs uppercase tracking-widest text-gold">{p.name}</p><p className="text-3xl text-white font-semibold mt-4">{formatCurrency(p.price*100)}<span className="text-xs text-noir-500 font-normal">/mês</span></p><p className="text-xs text-noir-500 mt-2">{p.storage} GB de armazenamento</p><ul className="mt-6 space-y-3">{p.features.map(x=><li key={x} className="text-sm text-noir-300">✓ {x}</li>)}</ul></article>)}</div></div>}
